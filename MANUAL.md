# TTS Generator Editor - 사용자 매뉴얼

## 개요
텍스트를 입력하면 PCM 오디오로 변환하는 과정을 시각적으로 확인할 수 있는 학습용 TTS 시스템입니다.

## 시작하기

### 요구사항
- Rust (1.75 이상)
- 웹 브라우저 (Chrome, Firefox, Safari 등)

### 실행 방법
```bash
# 프로젝트 루트에서
cargo run -p tts-server
```

서버가 시작되면 브라우저에서 `http://localhost:3000` 으로 접속합니다.

## UI 사용법

### 1. 텍스트 입력
- 화면 상단의 텍스트 입력 영역에 변환할 텍스트를 입력합니다.
- 예: "안녕하세요"

### 2. 언어 선택
- 드롭다운에서 언어를 선택합니다.
- 현재 **한국어**만 지원됩니다 (영어는 Phase 6에서 추가 예정).

### 3. 합성 실행
- "합성 실행" 버튼을 클릭합니다.
- 파이프라인의 각 단계가 순차적으로 활성화되는 애니메이션을 확인할 수 있습니다.

### 4. 파이프라인 시각화
화면에 TTS 파이프라인의 7단계가 표시됩니다:

| 단계 | 설명 | 상태 |
|------|------|------|
| 텍스트 입력 | 원본 텍스트 | ✅ 구현 |
| 텍스트 정규화 | 숫자, 기호를 한글로 변환 | 🔜 Phase 2 |
| 자모 분해 | 한글 → 초성/중성/종성 | 🔜 Phase 2 |
| 음소 변환 | 자모 → 음소(Phoneme) | 🔜 Phase 2 |
| 운율 생성 | 피치, 길이, 강세 부여 | 🔜 Phase 3 |
| 파형 합성 | Formant Synthesis | 🔜 Phase 4 |
| PCM 출력 | WAV 오디오 생성 | 🔜 Phase 4 |

### 5. 결과 확인
- 각 파이프라인 단계의 결과가 카드 형태로 표시됩니다.
- JSON 형식으로 상세 데이터를 확인할 수 있습니다.

### 6. 오디오 재생
- Phase 4 완료 후 파형 시각화 및 오디오 재생이 가능합니다.
- 현재는 빈 파형 캔버스만 표시됩니다.

## API 엔드포인트

| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/api/pipeline` | 파이프라인 단계 정보 조회 |
| POST | `/api/synthesize` | 텍스트 합성 요청 |

### POST /api/synthesize 요청 예시
```json
{
  "text": "안녕하세요",
  "language": "ko"
}
```

## 개발 현황
- **Phase 1:** ✅ 프로젝트 기반 구조 (현재)
- **Phase 2:** 한국어 텍스트 분석
- **Phase 3:** 운율 생성
- **Phase 4:** 파형 합성
- **Phase 5:** 튜닝 UI
- **Phase 6:** 영어 지원
- **Phase 7:** 최종 통합 테스트
