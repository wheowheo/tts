# TTS Generator Editor - 사용자 매뉴얼

## 개요
텍스트를 입력하면 PCM 오디오로 변환하는 과정을 시각적으로 확인할 수 있는 학습용 TTS 시스템입니다.

## 시작하기

### 요구사항
- Rust (1.75 이상)
- 웹 브라우저 (Chrome, Firefox, Safari 등)

### 실행 방법
```bash
# 프로젝트 루트에서
cargo run -p tts-server
```

서버가 시작되면 브라우저에서 `http://localhost:3000` 으로 접속합니다.

## UI 사용법

### 1. 텍스트 입력
- 화면 상단의 텍스트 입력 영역에 변환할 텍스트를 입력합니다.
- 예: "안녕하세요"
- 숫자도 입력 가능합니다 (자동으로 한글로 변환됨, 예: "123" → "일이삼").

### 2. 언어 선택
- 드롭다운에서 언어를 선택합니다.
- **한국어**와 **English** 모두 지원됩니다.

### 3. 합성 실행
- "합성 실행" 버튼을 클릭합니다.
- 파이프라인의 각 단계가 순차적으로 활성화되는 애니메이션을 확인할 수 있습니다.

### 4. 파이프라인 시각화
화면에 TTS 파이프라인의 7단계가 표시됩니다:

| 단계 | 설명 | 상태 |
|------|------|------|
| 텍스트 입력 | 원본 텍스트 | ✅ 구현 |
| 텍스트 정규화 | 숫자, 기호를 한글로 변환 | ✅ 구현 |
| 자모 분해 | 한글 → 초성/중성/종성 | ✅ 구현 |
| 음소 변환 | 자모 → 음소(Phoneme) | ✅ 구현 |
| 운율 생성 | 피치, 길이, 강세 부여 | ✅ 구현 |
| 파형 합성 | Formant Synthesis | ✅ 구현 |
| PCM 출력 | WAV 오디오 생성 | ✅ 구현 |

### 5. 결과 확인
- 각 파이프라인 단계의 결과가 카드 형태로 표시됩니다.
- **자모 분해**: 각 한글 문자가 초성(파란색)/중성(초록색)/종성(노란색)으로 분리되어 시각적으로 표시됩니다.
- **음소 변환**: 음소가 자음(파란색 칩)/모음(초록색 칩)으로 구분되어 표시되며, 하단에 음소 시퀀스 텍스트가 표시됩니다.
- **운율 생성**: 피치 곡선 그래프(빨간색 선)와 음소별 길이 바 차트가 표시됩니다. 총 길이, 기본 피치, 속도 정보도 확인 가능합니다.
- 그 외 단계는 JSON 형식으로 데이터를 확인할 수 있습니다.

### 6. 오디오 재생
- 합성 실행 후 파형(waveform)이 캔버스에 초록색으로 시각화됩니다.
- "재생" 버튼을 클릭하여 합성된 오디오를 브라우저에서 직접 재생할 수 있습니다.
- 파형 아래에 시간 표시(초 단위)가 표시됩니다.
- WAV 형식: 44100Hz, 16-bit, 모노.
- **A/B 비교**: 두 번 이상 합성 실행 시 "A/B 비교" 버튼이 나타나며, 이전 결과와 현재 결과를 번갈아 재생할 수 있습니다.

### 7. 튜닝 파라미터
합성 실행 전에 파라미터를 조정할 수 있습니다:

| 파라미터 | 범위 | 기본값 | 설명 |
|----------|------|--------|------|
| 기본 피치 | 80~300 Hz | 150 Hz | 목소리 높낮이 |
| 속도 | 0.5x~2.0x | 1.0x | 말하기 속도 |
| 볼륨 | 0.1~2.0 | 1.0 | 음량 |

#### 프리셋
- **기본값**: 150Hz, 1.0x, 볼륨 1.0
- **높은 목소리**: 220Hz
- **낮은 목소리**: 100Hz
- **빠르게**: 1.5x
- **느리게**: 0.7x
- **현재 값 저장/불러오기**: 커스텀 프리셋을 브라우저에 저장 (localStorage)

## API 엔드포인트

| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/api/pipeline` | 파이프라인 단계 정보 조회 |
| POST | `/api/analyze` | 텍스트 분석 (정규화 + 자모 분해 + 음소 변환) |
| POST | `/api/prosody` | 운율 생성 (피치, 길이, 강세) |
| POST | `/api/synthesize` | 텍스트 합성 요청 (전체 파이프라인) |

### POST /api/analyze 요청 예시
```json
{
  "text": "안녕하세요 123"
}
```

응답:
```json
{
  "original": "안녕하세요 123",
  "normalized": "안녕하세요 일이삼",
  "jamo": [
    {"character": "안", "choseong": "ㅇ", "jungseong": "ㅏ", "jongseong": "ㄴ"},
    ...
  ],
  "phonemes": [
    {"symbol": "a", "phoneme_type": "Vowel", "source_char": "안"},
    {"symbol": "n", "phoneme_type": "Consonant", "source_char": "안"},
    ...
  ]
}
```

### POST /api/synthesize 요청 예시
```json
{
  "text": "안녕하세요",
  "language": "ko"
}
```

## 한국어 텍스트 분석 상세

### 텍스트 정규화
- 숫자(0-9)는 한글로 변환됩니다 (0→영, 1→일, ..., 9→구)
- 쉼표(,)는 공백으로 변환됩니다
- 마침표(.), 느낌표(!), 물음표(?)는 유지됩니다
- 기타 특수문자와 영문은 제거됩니다

### 자모 분해
한글 음절을 유니코드 기반으로 초성/중성/종성으로 분해합니다:
- 초성 19개: ㄱ ㄲ ㄴ ㄷ ㄸ ㄹ ㅁ ㅂ ㅃ ㅅ ㅆ ㅇ ㅈ ㅉ ㅊ ㅋ ㅌ ㅍ ㅎ
- 중성 21개: ㅏ ㅐ ㅑ ㅒ ㅓ ㅔ ㅕ ㅖ ㅗ ㅘ ㅙ ㅚ ㅛ ㅜ ㅝ ㅞ ㅟ ㅠ ㅡ ㅢ ㅣ
- 종성 28개: (없음) ㄱ ㄲ ㄳ ㄴ ㄵ ㄶ ㄷ ㄹ ㄺ ㄻ ㄼ ㄽ ㄾ ㄿ ㅀ ㅁ ㅂ ㅄ ㅅ ㅆ ㅇ ㅈ ㅊ ㅋ ㅌ ㅍ ㅎ

### 음소 변환
자모를 IPA 기반 음소로 변환합니다:
- 초성 ㅇ은 무음으로 처리됩니다
- 초성 위치에 따라 유/무성음이 구분됩니다 (예: 어두 ㄱ→k, 어중 ㄱ→g)
- 종성은 대표음으로 변환됩니다 (예: ㄱ/ㄲ/ㅋ → k)

### 운율(Prosody) 생성
음소 시퀀스에 피치, 길이, 강세 정보를 부여합니다:
- 음소별 기본 길이: 모음 110~130ms, 자음 40~80ms
- 피치 곡선: 문장 초반 약간 상승 → 중간 유지 → 후반 하강 (한국어 평서문 패턴)
- 어절 경계에서 쉼(pause, 150ms) 삽입
- 속도 조절: speed_factor로 전체 길이를 비례 조정

### 파형 합성 (Formant Synthesis)
포먼트 합성 방식으로 PCM 오디오를 생성합니다:
- **모음 합성**: LF 글로탈 펄스(3단계: 개방+폐쇄+복귀) + 포먼트 공진기(F1/F2/F3)
- **자연스러움**: 지터(±1.5% 피치 변동) + 시머(±3% 진폭 변동) + 기식 노이즈
- **스펙트럼 기울기**: 1차 로우패스 필터로 고주파 감쇠 (~-12dB/octave)
- **자음 합성**: 마찰음(노이즈), 파열음(버스트), 비음/유음(LF 글로탈+공명)
- 음소 간 5ms 크로스페이드로 부드러운 전이
- 출력 정규화 (클리핑 방지)
- WAV 인코딩: RIFF 헤더 + 16-bit PCM 데이터

### 영어 지원
영어 텍스트를 규칙 기반 G2P(Grapheme-to-Phoneme)로 변환합니다:
- **사전 기반**: hello, world, the, is 등 자주 사용되는 단어 사전 내장
- **규칙 기반**: th, sh, ch, ph 등 다중 문자 패턴 인식
- **포먼트 테이블**: 영어 모음 15종 (iy, ih, eh, ae, aa, ah, ao, uh, uw, er, ey, ai, aw, ow, oy)
- 한국어와 동일한 합성 엔진을 공유합니다

## 개발 현황
- **Phase 1:** ✅ 프로젝트 기반 구조
- **Phase 2:** ✅ 한국어 텍스트 분석
- **Phase 3:** ✅ 운율 생성
- **Phase 4:** ✅ 파형 합성
- **Phase 5:** ✅ 튜닝 UI
- **Phase 6:** ✅ 영어 지원
- **Phase 7:** ✅ 최종 통합 테스트 완료

## 테스트 결과 (Phase 7)
- 단위 테스트: 18/18 통과
- 릴리스 빌드: 성공
- 한국어 E2E: "안녕하세요 반갑습니다" → 1.70초 WAV 생성
- 영어 E2E: "hello how are you" → 2.23초 WAV 생성
- 전체 API 엔드포인트: 5/5 HTTP 200
