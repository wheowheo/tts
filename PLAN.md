# TTS Generator Editor - 개발 계획

## 프로젝트 목표
텍스트를 입력하면 PCM 오디오로 변환하는 과정을 시각적으로 확인하고 튜닝할 수 있는 학습용 TTS 시스템.
Rust로 개발하여 최대 성능을 확보하고, REST API 기반으로 Web UI에서 실시간 확인 가능.

## 아키텍처 개요

```
[Web UI] <--REST--> [Rust Backend]
                        |
          +-------------+-------------+
          |             |             |
    [텍스트 분석]  [음소 변환]   [PCM 합성]
     (Phase 2)    (Phase 3)    (Phase 4)
```

### TTS 파이프라인
```
텍스트 입력
  → 텍스트 정규화 (숫자, 기호 → 한글)
  → 한글 자모 분해 (초성/중성/종성)
  → 음소(Phoneme) 변환
  → 운율(Prosody) 생성 (피치, 길이, 강세)
  → 파형(Waveform) 합성 (Formant Synthesis)
  → PCM 출력 (WAV)
```

---

## Phase 1: 프로젝트 기반 구조 (Scaffold)
**목표:** Rust 프로젝트 초기화, REST 서버, 정적 파일 서빙, 기본 UI 셸

- [ ] Cargo workspace 설정 (`tts-core`, `tts-server`)
- [ ] `tts-server`: Axum 기반 REST API 서버
- [ ] 정적 파일 서빙 (Web UI용 HTML/CSS/JS)
- [ ] 기본 UI 셸: 텍스트 입력 → API 호출 → 결과 표시 레이아웃
- [ ] `POST /api/synthesize` 엔드포인트 (스텁)
- [ ] `GET /api/pipeline` 파이프라인 상태 조회 (스텁)
- [ ] 개발 서버: `cargo run`으로 UI 즉시 확인 가능
- **검증:** 서버 시작, UI 로드, API 스텁 응답 확인
- **산출물:** 실행 가능한 서버 + 기본 UI

## Phase 2: 한국어 텍스트 분석
**목표:** 한국어 텍스트를 자모로 분해하고 음소로 변환

- [ ] `tts-core`: 텍스트 정규화 (숫자 → 한글, 특수문자 처리)
- [ ] 한글 유니코드 자모 분해 (초성/중성/종성)
- [ ] 음소(Phoneme) 매핑 테이블
- [ ] 음운 규칙 적용 (연음, 경음화, 비음화, 유음화 등)
- [ ] `POST /api/analyze` 엔드포인트: 텍스트 → 음소 목록 반환
- [ ] UI: 텍스트 분석 결과 시각화 (자모 분해 → 음소 변환 과정 표시)
- **검증:** "안녕하세요" → 정확한 음소 시퀀스 출력
- **산출물:** 텍스트 분석 파이프라인 + 시각화

## Phase 3: 운율(Prosody) 생성
**목표:** 음소 시퀀스에 피치, 길이, 강세 정보 부여

- [ ] 기본 운율 모델 (규칙 기반)
  - 음소별 기본 길이(duration) 테이블
  - 문장 내 위치 기반 피치 곡선
  - 어절 경계 감지 및 쉼(pause) 삽입
- [ ] `POST /api/prosody` 엔드포인트: 음소 → 운율 정보 반환
- [ ] UI: 운율 시각화 (피치 곡선 그래프, 음소별 길이 바 차트)
- [ ] UI: 운율 파라미터 수동 조정 슬라이더
- **검증:** 음소 시퀀스에 대한 운율 정보 생성 및 시각화
- **산출물:** 운율 생성 + 인터랙티브 시각화

## Phase 4: 파형 합성 (Formant Synthesis)
**목표:** 운율 정보를 기반으로 실제 PCM 오디오 생성

- [ ] Formant 합성 엔진
  - 한국어 음소별 포먼트 주파수 테이블 (F1, F2, F3)
  - 자음 합성 (노이즈 기반 마찰음, 파열음 등)
  - 모음 합성 (포먼트 필터)
  - 음소 간 전이(transition) 보간
- [ ] PCM 생성 (16-bit, 44100Hz)
- [ ] WAV 파일 인코딩
- [ ] `POST /api/synthesize` 완성: 텍스트 → WAV 바이너리
- [ ] UI: 파형(waveform) 시각화
- [ ] UI: 실시간 오디오 재생
- [ ] UI: 스펙트로그램 표시
- **검증:** "안녕하세요" → 재생 가능한 WAV 파일 생성
- **산출물:** 완전한 TTS 파이프라인 + 오디오 재생

## Phase 5: 튜닝 UI
**목표:** TTS 파라미터를 실시간으로 조정할 수 있는 인터랙티브 UI

- [ ] 전체 파이프라인 시각화 (단계별 입출력 표시)
- [ ] 포먼트 주파수 조정 UI
- [ ] 피치/속도/볼륨 글로벌 조정
- [ ] 개별 음소 파라미터 편집
- [ ] A/B 비교 재생 (조정 전/후)
- [ ] 파라미터 프리셋 저장/로드
- [ ] 실시간 미리보기 (파라미터 변경 즉시 반영)
- **검증:** 파라미터 조정 → 실시간 오디오 변화 확인
- **산출물:** 완전한 튜닝 인터페이스

## Phase 6: 영어 지원
**목표:** 한국어 파이프라인을 확장하여 영어 TTS 추가

- [ ] 영어 텍스트 정규화
- [ ] 영어 Grapheme-to-Phoneme (G2P) 변환
- [ ] 영어 음소 포먼트 테이블
- [ ] 영어 운율 규칙
- [ ] UI: 언어 선택 기능
- **검증:** "Hello World" → 재생 가능한 WAV 생성
- **산출물:** 이중 언어 TTS

## Phase 7: 최종 통합 테스트 및 정리
**목표:** 전체 시스템 검증 및 문서화 완료

- [ ] 전체 통합 테스트 스위트
- [ ] 성능 벤치마크
- [ ] MANUAL.md 최종 업데이트
- [ ] 코드 정리 및 주석

---

## 개발 규칙
1. 각 Phase 완료 시 → 검증 수행
2. 검증 통과 시 → MANUAL.md 업데이트 → 커밋
3. 전체 완료 시 → 전체 테스트 → 최종 커밋
4. 커밋 메시지: `Phase N: 설명` 형식

## 기술 스택
- **Backend:** Rust, Axum, tokio
- **Audio:** PCM 직접 생성 (외부 TTS 엔진 미사용)
- **Frontend:** Vanilla JS + Canvas API (빌드 도구 없이 즉시 사용)
- **시각화:** Web Audio API, Canvas 2D
